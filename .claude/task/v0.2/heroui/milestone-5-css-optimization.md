# Milestone 5: 全局 CSS 优化与最终整理

## 📋 里程碑概述

**优先级**：⭐⭐ 中
**预计时间**：2-3 小时
**状态**：🔲 待开始
**依赖**：Milestone 2, 3, 4 (组件迁移和 CSS 清理)
**阻塞**：Milestone 6 (测试验证)

## 🎯 目标

在前面几个里程碑完成组件迁移和即时 CSS 清理后，进行全局 CSS 架构的最终优化和整理。目标是统一导入结构、消除冗余、优化性能，并确保整体 CSS 架构清晰、可维护。

## 📊 当前状态（Milestone 2-4 完成后）

### 已删除的文件（在各里程碑中）

- ✅ `buttons.css` (Milestone 2)
- ✅ `forms.css` (Milestone 3)
- ✅ `modal.css` (Milestone 4)
- ✅ `sessions.css` (Milestone 4，如使用 ListBox)

### 已简化的文件（在各里程碑中）

- ✅ `chat.css` - 删除了输入框样式
- ✅ `sidebar.css` - 删除了 Tab 样式
- ✅ `version-card.css` - 删除了 BEM 覆盖

### 保留的文件（待优化）

- `globals.css` - **需要重构导入结构**
- `variables.css` - **待迁移到主题**
- `sidebar.css` - **待进一步简化**
- `chat.css` - **待进一步简化**
- `version-*.css` - **待优化**
- `markdown.css` - **保留不变**
- `scrollbars.css` - **保留不变**
- `tool-calls.css` - **保留不变**

## ✅ 任务清单

### 1. 迁移 variables.css 到主题

- [ ] **将所有设计令牌迁移到 `drawio2go.css` 主题**
  - 圆角系统已迁移 ✅ (Milestone 1)
  - 主题色已迁移 ✅ (Milestone 1)
  - 阴影系统已迁移 ✅ (Milestone 1)

- [ ] **评估保留项目特定变量**

  ```css
  /* 这些变量在主题外保留（如需要） */
  :root {
    /* 紧凑模式间距 */
    --spacing-compact-xs: 6px;
    --spacing-compact-sm: 8px;
    --spacing-compact-md: 12px;

    /* 紧凑模式字体 */
    --font-size-compact-xs: 11px;
    --font-size-compact-sm: 12px;
  }
  ```

- [ ] **删除或整合 `variables.css`**
  - 如所有变量已迁移，删除文件
  - 否则重命名为 `custom-variables.css` 并精简

### 2. 重构 globals.css 导入结构

- [ ] **创建新的 `globals.css` 结构**

  ```css
  /* app/globals.css */

  /* 定义 CSS 图层顺序 */
  @layer theme, base, components, utilities;

  /* 导入 Tailwind CSS */
  @import "tailwindcss";

  /* 导入 HeroUI 样式 */
  @import "@heroui/styles";

  /* 导入自定义主题 */
  @import "./styles/themes/drawio2go.css" layer(theme);

  /* 导入自定义变量（如需要） */
  @import "./styles/base/custom-variables.css" layer(base);

  /* 布局样式 */
  @import "./styles/layout/container.css" layer(components);
  @import "./styles/layout/sidebar.css" layer(components);

  /* 业务组件样式（仅保留特殊布局） */
  @import "./styles/components/chat.css" layer(components);
  @import "./styles/components/version-sidebar.css" layer(components);
  @import "./styles/components/version-wip.css" layer(components);
  @import "./styles/components/version-timeline.css" layer(components);
  @import "./styles/components/version-card.css" layer(components);

  /* 工具类样式 */
  @import "./styles/utilities/scrollbars.css" layer(utilities);
  @import "./styles/utilities/markdown.css" layer(utilities);
  @import "./styles/utilities/tool-calls.css" layer(utilities);
  ```

- [ ] **验证所有导入路径有效**
  - 检查所有文件存在
  - 无循环依赖

### 3. 优化保留的 CSS 文件

#### 3.1 优化 sidebar.css

- [ ] **移除已迁移的样式**
  - Tabs 样式已由 HeroUI 替代
  - 通用容器样式已由 HeroUI Surface 替代

- [ ] **保留核心功能**

  ```css
  /* 保留这些 */
  .resize-handle {
    /* 拖拽调整宽度 */
  }

  .sidebar-container {
    /* 侧边栏布局特定样式 */
  }
  ```

- [ ] **使用 CSS 变量替换硬编码值**

  ```css
  /* 旧代码 */
  background: #f5f5f5;
  border-radius: 8px;

  /* 新代码 */
  background: var(--surface);
  border-radius: var(--radius);
  ```

#### 3.2 优化 chat.css

- [ ] **确认输入框样式已删除**
  - `.chat-input-textarea` 已删除 ✅

- [ ] **保留业务特定布局**

  ```css
  /* 保留这些 */
  .message-user,
  .message-ai {
    /* 消息布局 */
  }

  .message-avatar {
    /* 头像定位 */
  }
  ```

- [ ] **使用 Tailwind 类替代简单样式**
  ```css
  /* 如果样式足够简单，删除 CSS，改用 Tailwind */
  /* 例如：flex gap-2 items-center */
  ```

#### 3.3 优化 version-sidebar.css

- [ ] **移除按钮样式**（已迁移到 HeroUI）

- [ ] **简化容器布局**
  ```css
  /* 仅保留特殊布局需求 */
  .version-list-container {
    /* 版本列表容器 */
  }
  ```

#### 3.4 优化 version-card.css

- [ ] **移除 BEM 覆盖**（已删除 ✅）

- [ ] **保留特殊时间线布局**
  ```css
  /* 仅保留 DrawIO 版本特有的布局 */
  .version-timeline-connector {
    /* 时间线连接线 */
  }
  ```

### 4. 消除剩余的 !important

- [ ] **搜索所有 !important**

  ```bash
  grep -r "!important" app/styles/
  ```

- [ ] **逐个分析并移除**
  - 通过提高选择器优先级
  - 通过调整 CSS 图层
  - 通过使用主题配置

- [ ] **记录无法移除的 !important**
  - 说明原因
  - 添加注释

### 5. 消除剩余的硬编码颜色

- [ ] **搜索主题色硬编码**

  ```bash
  grep -rE "#3388BB|rgb\(51, 136, 187\)|rgba\(51, 136, 187" app/styles/
  ```

- [ ] **替换为 CSS 变量**

  ```css
  /* 旧代码 */
  color: #3388bb;
  border-color: rgba(51, 136, 187, 0.2);

  /* 新代码 */
  color: var(--accent);
  border-color: color-mix(in oklab, var(--accent) 20%, transparent);
  ```

- [ ] **搜索其他硬编码颜色**

  ```bash
  grep -rE "#[0-9a-fA-F]{3,6}" app/styles/ | grep -v "^.*:.*//.*#"
  ```

- [ ] **评估是否需要替换**
  - 非主题相关的颜色可以保留
  - 与主题变化的颜色必须替换

### 6. 添加 CSS 文件文档

- [ ] **为每个保留的 CSS 文件添加头部注释**

  ```css
  /**
   * 文件名：chat.css
   * 用途：聊天界面业务特定样式
   * 依赖：HeroUI 基础样式、drawio2go 主题
   * 说明：仅包含无法用 HeroUI 组件实现的特殊布局和样式
   * 最后更新：2025-11-14
   */
  ```

- [ ] **添加关键样式块注释**

  ```css
  /* ========================================
   * 消息布局
   * ======================================== */
  .message-user {
    /* ... */
  }

  /* ========================================
   * 头像定位
   * ======================================== */
  .message-avatar {
    /* ... */
  }
  ```

### 7. 性能优化

- [ ] **合并小文件（可选）**
  - 评估是否将 `version-*.css` 合并为单个文件
  - 权衡：可维护性 vs 文件数量

- [ ] **优化选择器**
  - 避免过深的嵌套
  - 使用 BEM 提高效率

- [ ] **移除未使用的样式**
  ```bash
  # 使用 Chrome DevTools Coverage 检查
  # 或使用 PurgeCSS 分析
  ```

### 8. 最终文件结构验证

- [ ] **确认最终 CSS 文件结构**

  ```
  app/
  ├── globals.css                          # 全局入口
  └── styles/
      ├── themes/
      │   └── drawio2go.css               # 自定义主题
      ├── base/
      │   └── custom-variables.css        # 项目特定变量（可选）
      ├── layout/
      │   ├── container.css               # 容器布局
      │   └── sidebar.css                 # 侧边栏布局（优化后）
      ├── components/
      │   ├── chat.css                    # 聊天样式（优化后）
      │   ├── version-sidebar.css         # 版本侧边栏（优化后）
      │   ├── version-wip.css             # WIP 指示器
      │   ├── version-timeline.css        # 版本时间线
      │   └── version-card.css            # 版本卡片（优化后）
      └── utilities/
          ├── scrollbars.css              # 滚动条
          ├── markdown.css                # Markdown 渲染
          └── tool-calls.css              # 工具调用卡片
  ```

- [ ] **统计 CSS 指标**

  ```bash
  # 文件数量
  find app/styles -name "*.css" | wc -l

  # 总行数
  find app/styles -name "*.css" | xargs wc -l

  # 文件大小
  du -sh app/styles/
  ```

## 📝 实现细节

### CSS 图层说明

```css
/* 图层优先级（从低到高） */
@layer theme,      /* 主题变量，最低优先级 */
       base,       /* 基础样式和重置 */
       components, /* 组件样式 */
       utilities; /* 工具类，最高优先级 */
```

### CSS 变量命名规范

```css
/* HeroUI 变量（不要覆盖） */
--accent
--surface
--foreground
--radius

/* 项目特定变量（可自定义） */
--spacing-compact-*
--font-size-compact-*

/* 避免使用的名称（与 HeroUI 冲突） */
--primary /* 使用 --accent 代替 */
--background-color /* 使用 --background 代替 */
```

### 优化前后对比目标

| 指标            | 优化前（Milestone 1） | 优化后（Milestone 5） | 目标     |
| --------------- | --------------------- | --------------------- | -------- |
| CSS 文件数量    | 24 个                 | ~12-14 个             | ≤14 个   |
| CSS 总行数      | ~2800 行              | ~1500 行              | ≤1500 行 |
| !important 次数 | 多处                  | 0-2 次                | ≤5 次    |
| 硬编码颜色      | 多处                  | 0 次                  | 0 次     |
| 文件大小        | -                     | -                     | -40%+    |

## 🧪 验证标准

### 代码验证

- [ ] **CSS 文件数量符合预期**
  - 统计结果 ≤ 14 个

- [ ] **CSS 总行数符合预期**
  - 统计结果 ≤ 1500 行

- [ ] **!important 最小化**
  - 搜索结果 ≤ 5 次
  - 每个都有合理理由

- [ ] **硬编码颜色完全消除**
  - 搜索 `#3388BB` 无结果
  - 搜索 `rgb(51, 136, 187)` 无结果

- [ ] **所有 CSS 文件有文档注释**
  - 每个文件有头部说明
  - 关键样式块有注释

### 功能验证

- [ ] **所有页面样式正常**
  - 聊天界面无变化
  - 版本管理无变化
  - 设置面板无变化

- [ ] **主题切换正常**
  - 浅色 → 深色流畅
  - 无样式闪烁

- [ ] **响应式布局正常**
  - 侧边栏调整正常
  - 小屏幕布局正常

### 性能验证

- [ ] **CSS 加载时间**
  - 使用 DevTools Network 测量
  - 与优化前对比

- [ ] **CSS Coverage**
  - 使用 Chrome Coverage 工具
  - 未使用样式比例 < 30%

- [ ] **页面渲染性能**
  - Lighthouse Performance 评分 > 90
  - First Contentful Paint 无明显变化

## 📚 参考资源

- [CSS 图层规范](https://developer.mozilla.org/en-US/docs/Web/CSS/@layer)
- [CSS 性能优化](https://web.dev/fast/#optimize-your-css)
- [Chrome Coverage Tool](https://developer.chrome.com/docs/devtools/coverage/)

## ⚠️ 注意事项

1. **不要过度优化**
   - 可读性 > 文件大小
   - 可维护性 > 行数减少

2. **保留必要的自定义样式**
   - 业务特定的布局
   - HeroUI 无法实现的样式

3. **CSS 变量命名避免冲突**
   - 不要覆盖 HeroUI 变量
   - 使用项目前缀（如需要）

4. **测试所有页面**
   - 优化后必须逐页测试
   - 确保无样式破坏

5. **备份重要样式**
   - 创建 Git 提交点
   - 方便回滚

## 🔗 相关里程碑

- **依赖**：
  - Milestone 2: Button 迁移和 CSS 清理
  - Milestone 3: 表单迁移和 CSS 清理
  - Milestone 4: 复杂组件迁移和 CSS 清理

- **后续**：
  - Milestone 6: 测试验证

## 📝 完成标准

- [x] 所有任务清单项完成
- [x] CSS 文件数量 ≤ 14 个
- [x] CSS 总行数 ≤ 1500 行
- [x] !important 使用 ≤ 5 次
- [x] 硬编码颜色完全消除
- [x] 所有 CSS 文件有文档注释
- [x] globals.css 导入结构清晰
- [x] 所有验证标准通过
- [x] 代码已提交到 Git

---

**创建日期**：2025-11-14
**预计开始**：Milestone 2, 3, 4 完成后
**实际开始**：-
**完成日期**：-
